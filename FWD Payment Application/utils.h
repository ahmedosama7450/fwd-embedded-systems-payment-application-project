#pragma once

void inputString(char* msg, char* data);

int isPosDigit(int c);

// int inputInt(char msg[]);
//unsigned int inputUInt(char msg[]);
///* 
//	Input should have the following format: day-month-year
//	If input is in wrong format, -1 is returned
//*/
//int inputDate(char msg[], time_t* in);
